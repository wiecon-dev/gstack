[project]
name = "mermaid-render"
version = "1.0.0"
description = "OpenClaw mermaid-render skill dependencies"
requires-python = ">=3.10"
dependencies = [
    "playwright",
]

[tool.uv]
package = false
managed = true
python-preference = "system"