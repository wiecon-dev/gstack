interface:
  display_name: "scope-review"
  short_description: "Scope plan review with four modes: EXPANSION / SELECTIVE / HOLD / REDUCTION. Use when Adam asks for plan review, challenge proposal, scope check, or expand/reduce decision."
  default_prompt: "Use scope-review when Adam asks for plan review, challenge proposal, scope check, or expand/reduce decision. HARD GATE: Adam is 100% in control — never silently add or remove scope. Always ask for explicit opt-in per expansion."
policy:
  allow_implicit_invocation: false
