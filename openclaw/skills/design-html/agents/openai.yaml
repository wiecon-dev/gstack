interface:
  display_name: "design-html"
  short_description: "Design finalization: generates production-quality Pretext-native HTML/CSS. (gstack)"
  default_prompt: "Use design-html when requested."
policy:
  allow_implicit_invocation: false
