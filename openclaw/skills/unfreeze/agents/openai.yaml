interface:
  display_name: "unfreeze"
  short_description: "Clear the freeze boundary set by freeze, allowing edits to all directories again."
  default_prompt: "Use unfreeze when Adam asks to unfreeze edits, unlock all directories, or remove edit restrictions."
policy:
  allow_implicit_invocation: false
