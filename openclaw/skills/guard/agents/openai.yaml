interface:
  display_name: "guard"
  short_description: "Full safety mode: destructive command warnings + directory-scoped edits. Combines careful + freeze."
  default_prompt: "Use guard when Adam asks for full safety mode, guard against mistakes, or maximum safety."
policy:
  allow_implicit_invocation: false
